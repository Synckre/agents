# Documentos internos

Los archivos de esta carpeta se ingieren con el tag `internal`.
`front_agent` solo busca documentos con tag `public`; no uses este directorio para material que deba ver el chatbox.
