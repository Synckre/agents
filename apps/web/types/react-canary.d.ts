/**
 * Carga los tipos de React canary (ViewTransition, addTransitionType, ViewTransitionProps).
 * En runtime, Next.js aliasa 'react' a su build canary, por lo que el import funciona.
 */
import {} from "react/canary"
